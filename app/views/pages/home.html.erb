<div class="animatedParent">
  <div class="row valign-wrapper animated fadeInUp" > <!-- style="height:500px;background-image:url(http://www.darindaymusic.co.uk/wp-content/uploads/disco.jpg);margin-top:30px;padding-left:70px;padding-right:70px;background-size:cover"> -->
    <%= form_for :session, html: {class: "col s12 search-bar"} do |f| %>
    <%= f.text_field "City" , placeholder: "City", class:"col s3"%>
    <%= f.text_field "Performer" , placeholder: "Band", class:"col s3" %>
    <%= f.date_field "Date" , placeholder: "Date", class:"col s3" %>
    <%= f.text_field "Genre" , placeholder: "Genre", class:"col s3" %>
    <% end %>
  </div>
</div>
<br>

<div class="container opaque-background">
  <table class="bordered highlight responsive-table">
    <thead>
      <tr>
          <th data-field="Date">Date</th>
          <th data-field="Logo">Logo</th>
          <th data-field="Performer">Performer</th>
          <th data-field="Venue">Venue</th>
          <th data-field="Tickets">Tickets</th>

      </tr>
    </thead>

    <tbody>
      <% @concerts.each do |concert| %>
        <tr>
          <td> <%= concert.date %> </td>
          <td> <%= image_tag concert.logo %> </td>
          <td> <%= link_to concert.band, venue_concert_path(concert.venue.url, concert.url) %></td>
          <td> <%= link_to concert.venue.name, venue_path(concert.venue.url) %></td>
          <td> <%= link_to "Get Tickets!", venue_concert_path(concert.venue.url, concert.url) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
    <div class="col s5 offset-s3">
      <ul class="pagination center">
        <li class="waves-effect"><ahref="#!"><i class="material-icons">chevron_left</i></a></li>
        <li class="active"><ahref="#!">1</a></li>
        <li class="waves-effect"><ahref="#!">2</a></li>
        <li class="waves-effect"><ahref="#!">3</a></li>
        <li class="waves-effect"><ahref="#!">4</a></li>
        <li class="waves-effect"><ahref="#!">5</a></li>
        <li class="waves-effect"><ahref="#!"><i class="material-icons">chevron_right</i></a></li>
      </ul>
    </div>
  </div>
</div>
